package algo

func Minimax(gameInfo bool, node bool, depth int32, isMaximizingPlayer bool, alpha int32, beta int32) {
	// Recursively expand tree w/pruning
	// At leaf nodes compute MWR and roll back
	// Return best N results
}
